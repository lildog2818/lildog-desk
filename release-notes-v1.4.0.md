# v1.4.0

## 🚀 开机启动小组件重做

- **信息更全**：从图标网格改为列表卡片，每项同时展示名称、来源标签（注册表/启动文件夹）、完整命令预览；悬停/标题可见全路径
- **更易分辨**：来源改为彩色小徽标（HKCU/HKLM/启动文件夹）+ 行尾快捷按钮（复制命令、定位文件、更多操作）
- **已停用分区**：被关闭的启动项单独分区展示，支持一键恢复或移出
- **搜索过滤**：顶部新增搜索框，可按名称/命令/来源快速过滤

## 🎨 图标更稳

- 修复命令行引号解析 bug（未闭合引号、参数截断等）
- 启动文件夹的 `.lnk` 先解析出真实 exe 再取图标，避免显示空白或通用图标
- 新增后端批量接口 `startup_icon_sources`，列表一次性取图标源，减少往返
- 图标加载加入批量+去重+失败缓存，避免重复请求与闪烁

## ➕ 添加更顺手

- 新增「浏览…」与每行输入旁的 `…` 按钮，调用系统文件对话框选择程序（exe/lnk/bat/cmd）
- 支持把文件直接拖到输入框或列表区域，自动填入路径并根据文件名推断名称
- 输入行占位与底部提示更明确

## 🔧 其他

- 版本号升至 1.4.0
- 小组件默认尺寸调整为 400×520，更适合列表展示

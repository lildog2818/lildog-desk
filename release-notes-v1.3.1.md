# v1.3.1

## 🐛 控制台位置修复

- 修复控制台重启后位置偏移的问题：此前控制台在屏幕边缘（如贴右/贴底）时，重启后会向左/向上跳动一段距离
- 根因：重启时先以默认尺寸（400×620）计算边缘修正，再恢复实际尺寸，导致边缘判定错误
- 现改为**先恢复尺寸再计算位置**，与所有小组件的恢复顺序一致，控制台也能像其他小组件一样精确回到关闭前的位置与大小
- 隐藏到托盘/最小化按钮/退出三条路径的落盘逻辑保持不变

## 📦 其他

- 版本号升至 1.3.1
